<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.base.system.mapper.RegionMapper">

    <!-- 根据区划代码批量查询 -->
    <select id="selectByRegionCodes" resultType="com.base.system.entity.Region">
        SELECT * FROM sys_region
        WHERE deleted = 0
        AND region_code IN
        <foreach collection="regionCodes" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
        ORDER BY level, sort
    </select>

</mapper>
