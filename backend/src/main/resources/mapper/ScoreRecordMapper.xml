<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.base.stock.recommend.mapper.ScoreRecordMapper">

    <!-- 批量插入打分记录 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO stk_score_record (stock_code, rule_code, score_date, score, weighted_score, hit_flag, detail_json, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.stockCode}, #{item.ruleCode}, #{item.scoreDate}, #{item.score}, #{item.weightedScore}, #{item.hitFlag}, #{item.detailJson}, NOW())
        </foreach>
    </insert>

</mapper>
